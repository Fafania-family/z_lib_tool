# Z_Lib ブラッシュアッププラン

## 目的

Box Drive上の大量の小ファイルを個別に読む際の待ち時間を削減する。

ZIPを転送単位として取得し、ローカルに全展開してから処理する現在の方式を維持する。既存コードを少ない変更で利用できる透過APIも維持し、クラウドとの入出力、ローカル作業、保存の境界を明確にする。

## 目指す処理フロー

**読み取り**

```text
Box Drive上のZIP
  → ローカルへコピー
  → ローカルへ全展開
  → 大量ファイルをローカルで処理
  → 作業領域を解放、またはキャッシュとして保持
```

**編集**

```text
ローカルの展開済みデータを編集
  → ローカルで再圧縮・検証
  → 元ZIPの競合確認
  → Box Driveへ書き戻し
  → 成功後に作業領域を解放
```

Box Driveへの書き戻し完了と、クラウドへの同期完了は別の状態として扱う。

## 第1段階：性能の基準を作る

構造変更前に、実際の待ち時間がどこで発生しているか計測する。

| 比較対象 | 確認すること |
|---|---|
| Box Driveの展開済みフォルダを直接処理 | 現状の基準時間 |
| Box Drive上のZIPを直接開いて全展開 | 現実装の効果 |
| ZIPをローカルへコピーしてから全展開 | クラウドアクセスを取得時に集約する効果 |
| ローカルの展開済みキャッシュを再利用 | 繰り返し実行の改善幅 |

約100MB・10万ファイルの実利用に近いデータを対象に、取得、展開、実処理、再圧縮、書き戻しを分けて計測する。Box Driveの初回取得時とキャッシュ済みの状態も分ける。

**完了条件：** 初回と再実行の所要時間、必要なローカル容量、主要な待ち時間を説明できる。

## 第2段階：読み取り用途を確実にする

最優先で、解析するだけの処理が元ZIPを書き換えない設計にする。

- `load_zip()`・`swap_zip()` の既定を `mode="r"` に統一する。既存利用への変更点を明記する。
- 読み取りモードでは、ライブラリ経由の書き込み・削除・移動を操作時点で拒否する。
- ZIPをローカルへコピーし、展開処理はそのコピーに対して行う。
- 一時領域の配置先を設定可能にし、ローカルストレージの利用を前提にする。
- `swap_zip()` のパス比較を統一し、同じZIPの不要なアンロード・再ロードを防ぐ。
- 展開失敗時に途中データを片付け、展開先外への書き込みを拒否する。

`resolve()` で外部ライブラリへ実パスを渡す機能は維持する。ただし、その経路での変更はAPIだけでは防げないため、元ZIPへ保存しないことと、実パスの有効期間を明示する。

**完了条件：** 読み取り利用では元ZIPが変化せず、展開後のファイル処理がローカルだけで完結する。

## 第3段階：編集と保存を分離する

保存をアンロードや終了処理だけに依存させず、利用者が結果を確定できるようにする。

- `commit()`：編集内容を保存する。
- `rollback()`：編集内容を破棄する。
- `close()`：定義された方針で作業領域を解放する。
- コンテキストマネージャーに対応し、例外時は自動保存しない。
- 再圧縮と検証はローカルで完了させてから書き戻す。
- 保存失敗時は編集データを保持し、復旧先を通知する。
- 元ZIPが取得後に変更された場合は、上書きを止めて競合を通知する。
- 空ディレクトリを保持し、ZIPの属性・コメントなどの保持範囲を定義する。

Box Drive上での置換手順は実環境で検証する。別端末からの同時更新まで完全に防ぐ保証は設けず、競合検出の限界を明記する。

**完了条件：** 例外や保存失敗で編集データが失われず、保存・破棄を利用者が選択できる。

## 第4段階：必要に応じてキャッシュを導入する

再展開時間が実測で問題になる場合に実施する。

- 元ZIPの識別情報と更新情報を記録し、再利用可否を判断する。
- 読み取り用キャッシュと編集用作業領域を分ける。
- 外部へ書き込み可能な実パスを渡した領域は、そのまま共有キャッシュとして再利用しない。
- 容量上限・有効期限・明示的な削除操作を設ける。
- 使用中の領域を削除しない仕組みを設ける。
- 中断された取得・展開結果を完成済みとして扱わない。

**完了条件：** 同じZIPの再処理が短縮され、古い内容や途中データを誤って再利用しない。

## 内部構造

現在の名前空間分割を活かし、処理境界に沿って責務を整理する。

| 責務 | 担当する処理 |
|---|---|
| 公開API | `load_zip`、`open`、`os`、`shutil` など |
| パス解決 | ローカル／ZIP内の判定、正規化、境界検証 |
| セッション管理 | モード、状態、作業領域の寿命、保存・破棄 |
| ZIP処理 | 展開、再圧縮、検証、エントリ情報の保持 |
| 転送処理 | 元ZIPのローカル取得、更新確認、書き戻し |
| キャッシュ管理 | 再利用、容量制御、使用中管理。第4段階で追加 |

バックエンドを差し替えて検証できる構造にし、各名前空間から内部のロード管理辞書を直接操作しないようにする。

## 利用体験

- 常時出力するログを任意にし、取得・展開・処理可能・圧縮・書き戻しの進捗を通知する。
- ロード済みZIP、モード、作業領域、保存失敗状態を確認できるAPIを用意する。
- 通常操作は仮想パスを返し、実パスの取得は `resolve()` に集約する。
- `os.walk()` の探索対象変更など、標準APIに似せた部分の挙動を揃える。
- READMEを「Box Drive上の大量小ファイル処理」という用途を中心に書き直す。

## 検証項目

代表データの性能計測に加え、以下を回帰検証する。

- 読み取り後に元ZIPが変更されない。
- 相対・絶対パスを混ぜても同じロード状態を維持する。
- 日本語名・空ディレクトリを保持する。
- 不正な展開先を拒否する。
- 展開途中の失敗を回収できる。
- 保存失敗時に編集内容を復旧できる。
- 元ZIPの変更を検出する。
- `resolve()` の寿命とキャッシュ再利用の整合性を守る。

## 実装優先順位

**性能計測 → 読み取り専用とローカル取得 → 保存・復旧の確立 → 必要に応じたキャッシュ導入**

初期の対象から、GUI、Box APIとの直接連携、ZIP内ファイルの都度取得、複数端末での同時編集は外す。まず、現在の強みである「一度取得すれば大量のファイルをローカルで扱える」処理を確実にする。