# Z_Lib 開発計画書・アーキテクチャ設計書

## 1. 概要

Z_Lib は、Pythonにおいて標準ファイルシステム（ローカルファイル）とZIPアーカイブ内のファイルを、パス文字列の記述だけで完全に透過的かつシームレスに扱うためのラッパーライブラリである。

特に **Box Drive等のクラウド同期ストレージ環境における大量の小ファイル処理** において、同期遅延や個別I/O待ち時間を極小化するため、ZIPアーカイブを転送単位としてローカル一時領域に全展開し、高速なローカルアクセスと安全なトランザクション管理を提供する。

---

## 2. コア・コンセプト

1. **二段階転送によるクラウドアクセスの集約**
   Box Drive等のリモートパス上のZIPを直接解凍するのではなく、まずローカル作業領域に一括コピーし、ローカル上で全展開を行う。これにより、クラウドストレージ特有のオンデマンドフェッチ遅延や細切れ通信を排除する。
2. **安全第一の読み取り専用設計（既定 `mode="r"`）**
   解析目的の処理が元ZIPを破壊しないよう、ロード既定値を `"r"` に統一。読み取りモード時は `open(..., "w")` や `os.remove`、`shutil.copy2` などの変更操作をAPI呼び出し時点で確実にブロック（`ZipReadOnlyError`）する。
3. **明示的なトランザクション（編集・確定・破棄の分離）**
   変更の保存を終了時やアンロードに依存させず、`commit()`（保存）、`rollback()`（破棄）、`close()`（解放）として分離。コンテキストマネージャー（`with z.edit(...)`）に対応し、例外発生時は元ZIPを破壊せずに未保存データを安全にロールバックする。
4. **自己検証とアトミックな置換**
   再圧縮処理はローカルで完了させ、`zipfile.testzip()` による破損検証を通過した後にのみ、元ZIPへのアトミック置換（同一親ディレクトリでの一時ファイル作成＋`os.replace`）を行う。
5. **更新競合検知 & データ退避保護**
   ロード時に元ZIPのタイムスタンプ・サイズを記録し、コミット時に外部更新を検知した場合は上書きを中断して `ZipConflictError` を送出。また万一の書き戻し失敗時も編集データは消去せず、`ZipSaveError.recovery_path` として保護・通知する。
6. **完全な日本語文字エンコーディング対応**
   Windows環境のCP932（Shift-JIS）ダメ文字（0x5C:「表」「能」など）を含むファイル名を、Local File Headerの生バイト列直接抽出により100%正確に復元する。

---

## 3. モジュール構成と責務

| モジュール | 責務 | 主な構成要素 |
|---|---|---|
| `core.py` | 公開API、全体統括、ライフサイクル管理 | `Z_Lib` (load_zip, swap_zip, commit, rollback, close, edit, open, resolve) |
| `session.py` | 1つのZIPのライフサイクル・状態・作業領域管理 | `ZipSession` (load, commit, rollback, close, check_writable, mark_dirty) |
| `transfer.py` | クラウド・ローカル間のファイル転送、メタデータ取得、置換 | `copy_to_local`, `get_file_metadata`, `check_conflict`, `atomic_replace` |
| `path_resolver.py` | パス正規化（大文字小文字・スラッシュ）、セッション照合、境界検証 | `normalize_path`, `normalize_lookup_key`, `resolve_to_real_path`, `split_zip_path` |
| `backend/zipfile_backend.py` | ZIP展開、圧縮、testzip検証、CP932ダメ文字対応、空ディレクトリ保持 | `extract_zip_safely`, `compress_directory_to_zip`, `_decode_zip_filename` |
| `namespaces/` | 標準ライブラリモ倣インターフェースと操作ガード | `Z_OS`, `Z_OS_Path`, `Z_Shutil` |
| `exceptions.py` | エラー体系 | `ZipReadOnlyError`, `ZipConflictError`, `ZipSaveError`, `ZipSecurityError` 等 |
| `_types.py` | 型定義 | `OpenMode`, `ZipHandle`, `ProgressCallback`, `SessionStatus`, `FileMetadata` |

---

## 4. 処理フロー

### 4.1 読み取りフロー（既定）
```text
Box Drive上のZIP
  → ローカル一時領域へ一括コピー (transfer.copy_to_local)
  → 文字コード・Zip Slip安全検証を伴う全展開 (backend.extract_zip_safely)
  → 透過API / resolve() 経由でローカルSSD上で超高速処理
  → close() で一時領域を安全に解放（元ZIPは不変）
```

### 4.2 編集・トランザクションフロー
```text
with z.edit("data.zip"):
    ローカルの展開済みデータを編集
    （変更が加わると is_dirty フラグが True になる）
  → [正常終了時]
      1. 元ZIPの外部更新チェック (check_conflict)
      2. ローカル一時領域でZIP再圧縮 (compress_directory_to_zip)
      3. testzip() による整合性自己検証
      4. 同一ディレクトリ内一時ファイル経由のアトミック置換 (atomic_replace)
  → [例外発生時]
      rollback() を実行し、元ZIPを一切更新せずに作業領域を安全に破棄
```

---

## 5. 日本語文字エンコーディング対応（CP932 ダメ文字問題）

### 5.1 背景
Shift-JIS (CP932) では、「表」（`0x95 0x5C`）や「能」（`0x94 0x5C`）などの2バイト目に `0x5C`（ASCIIのバックスラッシュ `\`）が含まれる。
Python標準の `zipfile` モジュールは、これをディレクトリ区切り文字と誤認して `/`（`0x2F`）に自動置換するため、ファイル名が文字化け・階層化して破損する。

### 5.2 解決策
`_decode_zip_filename()` において：
1. `zipfile.ZipInfo` の `header_offset` を参照してZIPストリームの「Local File Header」にシーク。
2. ライブラリによる置換前の **生のファイル名バイト列** を直接抽出。
3. 抽出した生バイト列に対して `cp932` デコードを適用。
これにより、Windows標準機能で作成された日本語ZIPも正確に復元できる。
